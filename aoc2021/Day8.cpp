#include <iostream>
#include <math.h>
#include <string>
#include <vector>
#include <fstream>
using namespace std;

class Part1
{
    public:
        
};

int main()
{
    ifstream file("inputDay8.txt");
    string input;
    vector<string> output;
    while (getline(file, input, '\n'))
    {
        stringstream ss(input);
        
    }

        return 0;
}